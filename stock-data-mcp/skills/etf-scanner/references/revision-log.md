# ETF Scanner 版本变更记录

## v2.3.0

行为更新：
1. 全量文档切换为 MCP-only 消费规范。
2. 移除 skill 范围内所有计算公式与本地推导描述。
3. 明确 `etf_batch_decide` 为唯一计算源。
4. 强化关键字段缺失即失败与中止策略。
5. 清理历史旧口径样例，避免污染后续输出。

兼容性说明：
- 输出五段结构与动作字典保持不变。
- 仅收紧责任边界与文档口径。

## v2.2.2

行为更新：
1. 评分、仓位、动作归因计算下沉至 `stock-data__etf_batch_decide`。
2. Skill 主流程改为 MCP-first：消费结构化 `results/watchlist/errors/globalChecks`。
3. 终止场景明确不回退旧四工具流程，避免双口径结果混用。

## v2.2.1

行为修复更新：
1. 删除组合暴露判断口径，仅保留单标的暴露语义。
2. 动作主限制因子收敛为统一枚举。
3. 输出协议补充暴露口径与误判防护说明。
